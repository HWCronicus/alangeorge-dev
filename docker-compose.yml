services:
  app:
    container_name: alangeorge-dev-app
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8080:8080"
      - "42069:42069"
    env_file:
      - .env
    restart: unless-stopped
